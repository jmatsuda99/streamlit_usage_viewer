# Daily Usage Viewer (30-min) — Unit Toggle

- English labels
- Unit toggle: **kWh (per 30 minutes)** or **kW** (converted by ×2)
- Data persisted to SQLite; upload once and reuse

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```